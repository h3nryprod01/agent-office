{
  "name": "@agent-office/renderer",
  "version": "0.0.1",
  "description": "PixiJS isometric office renderer — placeholder graphics, driven by a mock event stream",
  "type": "module",
  "private": true,
  "scripts": {
    "dev": "vite",
    "build": "tsc --noEmit && vite build",
    "preview": "vite preview",
    "test": "vitest run",
    "test:watch": "vitest"
  },
  "dependencies": {
    "@types/three": "^0.171.0",
    "three": "^0.171.0"
  },
  "devDependencies": {
    "typescript": "^5.7.3",
    "vite": "^6.1.0",
    "vitest": "^3.0.5"
  },
  "license": "MIT"
}
